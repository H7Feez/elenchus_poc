node_modules/
*.vsix
.DS_Store

# The benchmark dataset has no licence file, so it is fetched, not vendored.
# Trained weights are large and reproducible from the scripts.
model/data/
model/out/
__pycache__/

# Built by build_space.py; duplicated weights, not source
model/space/
